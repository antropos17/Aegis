# Backend and frontend audit — 2026-09-09

This records the backend/frontend audit and its consistency follow-up. The later all-section duplicate pass has its own results in SECTION-DUPLICATES-2026-09-09.md. Earlier integration reports retain historical results; they are not evidence for later patches.

## Scope and confirmed changes

Three parallel audits covered resource/token collection, frontend mutation workflows, and IPC/settings/export security. A separate implementation pass checked host freshness and custom catalog recognition. Network monitoring received a bounded read-only review without a newly reproduced defect.

- Missing or malformed CPU/RAM values remain unavailable through normalization and cache reads.
- Inherited JavaScript model names use fallback pricing; invalid token rows and thrown non-Error values cannot stop remaining adapters or poison costs.
- Settings saves preserve newer drafts; provider requests cannot clear a reopened editor; catalog deletions and permission reset/save actions cannot overlap.
- Successful scan-interval changes update the renderer freshness deadline; superseded startup responses and errors cannot overwrite live data.
- Settings validate every persisted field, nested permissions, custom signatures, exceptions and watchlist before mutation. Invalid disk fields recover to defaults while valid preferences survive.
- API keys are encrypted before persistence. Unavailable or Linux basic_text storage cannot write plaintext. Locked encrypted keys survive unrelated saves and failed replacements; explicit removal has a separate save option.
- Synchronous custom regex matching accepts a conservative subset: no repeated groups, backreferences, lookarounds or multiple variable repetitions. Previously stored unsupported patterns are skipped.
- CSV exports prefix formula-like text cells and escape carriage returns.
- Validated custom catalog signatures participate in each process scan. Built-in identity/process-name ownership takes priority; catalog edits do not modify the bundled database or identity stamping.

## Verification

Local verification passed: 3098 tests and 4 skips across 176 files with coverage; format, lint (0 errors, 62 warnings), all TypeScript/Svelte projects, witness/sequence mutation gates, inventory, production dependency audit (0 vulnerabilities), and both renderer builds. Browser QA passed 464 view/theme/scale/section states plus interaction checks, preserving all twelve reference stylesheets.

The packaged Windows executable observed 25 processes and passed eleven workspaces, settings restart, six native exports and key-free configuration round trip with no renderer errors. All 193 packaged runtime files, including 114 renderer files, match the checked source/build byte for byte. Package: dist/audit-package/win-unpacked. ASAR SHA256: a8b1025637e3466ff12f163fd9f286adb2c12299b3817f45ec2fe2253bdc9fc2.

PR #412 merged after all five required CI contexts passed. After explicit installation approval, its verified package was installed and launched with application/profile backups. Installed live QA matched CPU to delivered measurements, checked 22 metrics, five source observations, pause/resume and unchanged preferences with no renderer errors.

## Remaining limits

At the end of PR #412, cache replays still used delivery time and full settings snapshots could conflict across windows; the follow-up below addresses these two limits. Token telemetry currently has a Claude Code adapter; unsupported providers remain unavailable/partial. Native macOS/Linux execution, paid provider calls, and actions against user processes were not exercised.

The broader security review also noted sender/navigation/process-control hardening opportunities without a confirmed end-to-end exploit. No claim of exhaustive security coverage or user visual approval is made.

## Settings and resource consistency follow-up

Settings and provider editors now send only changed fields with patch semantics. The main process validates and merges against its current settings before committing; legacy/import full replacement remains supported. Concurrent edits to separate fields survive, including API key changes. Unchanged appearance fields cannot overwrite another window or a newer local preview. Two writes to the same field still use the last committed value.

Resource rows carry collection start/completion times and a monotonically increasing collection sequence. Cache TTL starts on completion; replay keeps original metadata and an older asynchronous result cannot replace a newer cached or displayed instance. Completed queries may fill the cache while later queries are pending, avoiding cache starvation under overlapping requests. Charts add a numeric point only for a new collection. Composite totals expose the span of their per-instance collection times. Missing identity or malformed provenance produces unavailable coverage instead of an invented measurement. Backwards wall-clock changes start a fresh timeline and reset rate baselines without replaying unchanged old telemetry. Collector times describe collection, not exact OS sampling.

Final local coverage passed 3143 tests with 4 skips across 179 files. Format, lint (0 errors, 62 warnings), TypeScript/Svelte, inventory, witness/sequence mutation gates and production dependency audit passed. Renderer production/preview builds and 464 browser states passed. Windows package QA observed 27 processes, checked eleven workspaces, settings across restart, six exports and the key-free configuration round trip without renderer errors. A separate disposable-profile run at a one-second scan interval confirmed that two cache replays added no points and a fresh collection advanced the graph from one to two points.

All 193 packaged runtime files, including 114 renderer files, match the checked source/build byte for byte. Package: dist/consistency-package/win-unpacked. ASAR SHA256: 91ab58c06e8ccd4cd4477e788aa03b0e6c04942e78fc1dfaa24cdc63a84f8b96. The package is installed at X:/Future/ESCAPE/AEGIS-Desktop with the previous application in X:/Future/ESCAPE/AEGIS-Desktop-before-consistency-20260909 and profile backup in X:/tmp/aegis-consistency-profile-backup-20260909. Installation preserved settings bytes. Installed-profile live verification matched CPU to all 26 measured processes and passed 22 metrics, six source observations, pause/resume and preserved preferences with no renderer errors. CI/merge status is recorded in the PR and desktop handoff after the final run.
