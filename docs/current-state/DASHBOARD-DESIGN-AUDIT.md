# Сравнение дашборда и полировка — 19 сентября 2026

Сравнение выполнено с Observatory на `12eee9f`, актуальным `origin/master` на
момент начала работы. Основная рабочая папка содержит более старую версию и
незавершённые правки; они сохранены. Изменения сделаны в отдельной ветке
`codex/dashboard-design-polish`.

Референс: пользовательский файл «AEGIS — идеальный дашборд.html», 18 экранов и
компонентных листов. SHA-256:
`581453cbe7be046a1f6167fde877d09a10c74c6e8a279f980e7d7728fadc5143`.
Его содержимое рассматривается как дизайн-референс; изображённые данные,
состояния сенсоров и кнопки не подтверждают наличие соответствующих возможностей.

## Вывод

Референс лучше выделяет конкретную задачу: выбрать наблюдение, понять его
основания и перейти к действию. Нынешний интерфейс уже предоставляет большую
часть рабочих разделов, сохраняет выбор агента и формы, различает реальные
наблюдения и демонстрацию. Основная подтверждённая проблема обзора — распределение
места: повторные вводные и крупная сводка оттесняли первую запись, а открытое
доказательство получало меньшую ширину, чем список.

Полировка переносит эти принципы в существующий обзор. Она не заменяет интерфейс
целым статическим макетом и не меняет стартовый радар.

## Сравнение

| Область | Референс | Observatory до правки | Решение |
| --- | --- | --- | --- |
| Главный экран | Review с очередью внимания | Стартовый радар, рядом Protection overview | Сохранён выбор стартового экрана; улучшен обзор |
| Иерархия обзора | Краткая сводка, затем записи | В 900×600 текст первой записи скрывался нижней границей рабочей области | Уплотнены сводка и заголовки; убрана повторная вводная в низком окне |
| Выбранная запись | Широкая область доказательств | Соотношение списка и деталей 1,6:1 | Деталям отдана большая ширина; на узких окнах сохранён последовательный порядок |
| Следующее действие | Кнопки рядом с доказательством | Evidence и Agent & controls ниже блока политики | Кнопки подняты перед сохранённой политикой |
| Ограничения знания | Явное различие наблюдения и вывода | Оговорка об открытом дескрипторе внутри закрытого disclosure | Оговорка, время и число записей доступны сразу |
| Цветовые акценты | Выделено требующее внимания | Отсутствие автоматической блокировки также окрашено янтарным | Штатное ограничение нейтральное; текст остаётся видимым |
| Фильтры | Счётчики категорий | Только названия | Добавлены счётчики групп, включая доступное описание кнопки |
| Навигация | Компактные Monitor / Check before use / Records / Configure | Observe / Investigate / Assess / Configure, больше отдельных назначений | Полная перегруппировка выходит за полировку: потребуется сохранить команды, историю и выбор разделов |
| Агенты и активность | Список с раскрытием, граф доказательств, Files/Network в одной области | Таблица агентов, отдельная рабочая область с вкладками, отдельные Events/Network | Существующие пути сохранены; граф доказательств требует отдельного контракта связей и причинности |
| Локальная проверка и действия | Результат перед подробностями настройки | Уже есть сохранённый результат, ограничения и отдельное live route observation | Новая копия этих экранов не нужна; перенос показанных в макете результатов недопустим |
| Записи и отчёты | Общая группа Records, предпросмотр отчёта | Audit и Reports в разных группах; экспорт и просмотр сохранённых записей | Единая навигация и предпросмотр — отдельная функциональная работа |
| Commands | Поиск агентов, файлов, цепочек и разделов | Поиск разделов и задач через `WorkspaceCommand[]` | Поиск сущностей потребует отдельной индексации; текущее название поиска сохранено |
| Сенсоры | Сводка потерь и состояния с временными отметками | Sensors ведёт в существующую статистику сенсоров | Значения и гарантии покрытия из референса не перенесены |

## Что осталось за пределами полировки

Общая очередь последовательностей и неприсвоенной активности, дневной счётчик
рассмотренных записей и поиск сущностей требуют работы с данными. Кнопка
`Mark reviewed` существует в деталях экземпляра процесса: отметка хранится только
в памяти текущей сессии по `instanceId`. Это не отметка рассмотренной записи
активности и не сохраняемая очередь Review из референса. Открытие агента, точная
идентичность процесса, отсутствие наблюдения, пауза и отказы чтения предпочтений
сохраняют прежние правила.

## Проверки

- Визуально просмотрены 18 референсных листов, текущие основные разделы и
  изменённый обзор. Сравнение широких экранов выполнено при 1440×900.
- Дополнительная матрица обзора: 32 сочетания 900×600 / 1200×800, масштаба
  100 / 150%, четырёх тем, английского и португальского. Проверены выбор Enter,
  возврат фокуса, пустая выдача и отсутствие горизонтального переполнения.
- В 900×600 / 100% первая строка занимает y=406,78–498,28; нижняя строка
  приложения начинается на y=570. Первая запись целиком видна без прокрутки.
  При 150% содержимое остаётся доступным через обычную прокрутку.
- У пяти выбранных текстовых ролей измерены фактические цвета в четырёх темах:
  минимальное отношение контраста 5,22:1. Это выборочная проверка изменённой
  области, не сертификация доступности всего приложения.
- Компонентная регрессия проверяет видимость оговорки об открытом дескрипторе
  и времени без раскрытия подробностей; прежние проверки устаревших данных,
  смены экземпляра процесса и отказа чтения предпочтений сохранены.
- Браузерная регрессия проверяет видимость первой строки в маленьком окне и
  приоритет ширины доказательств; включает 24 комбинации размера/темы/масштаба
  с выбором записи и без него.

Скриншоты и машинные результаты этой сессии находятся в игнорируемых
`out/design-audit/` и `dist/frontend-qa/`. Полные проверки репозитория записаны
в результатах PR. Нативный Electron, живые сенсоры и экранный диктор в этой
полировке отдельно не проверялись; браузерная демонстрация не подтверждает их работу.

## Повторная проверка PR #520

Проверен merge-коммит `192108820b1a2ee19ea55219a364dd8c2a537d4a`;
его PR CI и отдельный CI master завершились успешно. Полировка не меняет
стартовый радар и не реализует весь статический референс.

Подтверждён дефект восстановления фокуса в существующем обработчике закрытия
деталей. В 1200×800 / 150% после выбора восьмой строки и закрытия панели фокус
оказывался на строке y=1123,73–1244,48 при нижней границе рабочей области 762,25.
Обработчик фокусировал строку до перестройки сетки и запрещал прокрутку. Если
выбранную строку скрыть поиском, после закрытия фокус терялся полностью.
Этот обработчик существовал до #520; новые проверки закрывают пробел прежней
проверки только первой строки и одного факта `activeElement`.

Исправление ожидает обновление Svelte, возвращает фокус и прокручивает к строке
с учётом отступа под закреплённым заголовком. Если строки уже нет, фокус получает
раздел активности. Скрытая рабочая область не перехватывает фокус. После правки
та же восьмая строка занимает y=625,73–746,48 и целиком видна.

Повторная проверка прошла 32 сочетания 900×600 / 1200×800, масштаба 100 / 150%,
четырёх тем и двух языков. Выбор и закрытие выполнялись клавишей Enter;
проверены видимая строка с фокусом, резервный фокус после поиска и отсутствие
горизонтального переполнения. Общий `frontend:test` также прошёл. Нативный
`frontend:test:electron` проверил 11 разделов, надёжный снимок процессов,
перезапуск, сохранение настроек и шесть обработчиков экспорта без ошибок
рендерера, вмешательства в процессы или запросов к провайдерам. Экранный диктор
отдельно не проверялся. Артефакты этой проверки находятся в игнорируемых
`.agent/redesign-520/`, `dist/frontend-qa/` и `dist/electron-qa/`.
