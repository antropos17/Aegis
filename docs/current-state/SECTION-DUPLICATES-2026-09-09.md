# Sections and duplicate handling — 2026-09-09

This pass follows merged PR #413. Its scope is duplicate presentation and agreement between workspaces, retained observations and exports. Historical UI plans are not design sources.

## Section review

| Section | Finding and resulting behavior |
| --- | --- |
| Monitoring | Token totals use the current stamped population and show measured coverage. Sensitive counts match the retained records opened by the card. |
| Agents | Different stamped processes belonging to one product remain separately accessible. The native baseline did not reproduce duplicate stamped rows. |
| Statistics | Partial token readings agree with graph subtotals. Processes uses Comparison / Table so the same products are not listed twice in succession. |
| Events | Windows path spelling and different actions no longer split one resource/owner into look-alike groups. Every original observation remains available, with mixed evidence and activity labels. |
| Network | Distinct local TCP endpoints and states remain distinct records. Unknown local endpoints never establish equality. Radar resource groups retain every connection. |
| Audit | Action, attribution and classification changes survive backend suppression. Saved network metadata is displayed from details, with extra supported for compatible hosts. |
| AI analysis | Counts come from the captured request scope; opening HTML preserves the same risk level and counts. |
| Reports | Shared resource groups and evidence summaries agree with Events. CSV and HTML preserve local endpoint distinctions. |
| Rules & permissions | One editable target per durable project policy, including inactive saved projects, with backend-compatible inheritance. Individual saves use the atomic existing permission API. |
| Agent catalog | Repeated signatures cannot break keyed rendering. Existing bundled/custom ID collisions remain visible as inactive custom definitions. |
| Settings | Provider setup remains a link to its single editor; no duplicate settings controls were reproduced. |
| Notifications and details | Product-level anomaly notifications remain intentional. Radar groups open all retained records; socket records show their local ports. |

The native baseline traversed 30 workspace/inner-section states with no duplicate DOM IDs, exactly repeated visible table rows, duplicate stamped input keys or renderer errors. Synthetic fixtures reproduced the grouping, counter and evidence defects above; an absence of duplicate keys in that one native run does not prove every possible input unique.

## Preservation boundaries

Display grouping retains original records, including different process lifetimes, action kinds and evidence. Canonical Windows display keys do not modify recorded paths or runtime identity; POSIX path case stays distinct. Resource directory context never supplies actor attribution. File suppression retains its existing time window but requires semantic equivalence. Network sampling does not invent socket lifetimes.

Native macOS/Linux execution, paid provider requests and interventions against user processes are outside this pass. POSIX parser behavior is checked with fixtures. Two edits of the same policy still use the last committed value.

## Verification

Repository checks passed: 3203 tests and 4 skips across 182 files with coverage; format, lint (0 errors, 56 warnings), all TypeScript/Svelte projects, witness and sequence mutation gates, indexed inventory, production dependency audit (0 vulnerabilities), production renderer and preview builds.

The packaged Windows executable passed all 11 workspaces, reliable live population, hardened BrowserWindow settings, settings restart, six exports and key-free configuration round trip. A separate native duplicate traversal covered 32 workspace/inner-section states: no exactly repeated visible table rows, duplicate DOM IDs, duplicate stamped input keys, horizontal overflow or renderer errors. All 193 runtime files (including 114 renderer files) match the checked source/build bytes.

Browser QA passed 464 view/theme/scale/section states plus interaction checks, including the grouped radar drilldown with both original records. All twelve reference stylesheets retain their source hashes and cascade order.

The verified Windows package is installed at X:/Future/ESCAPE/AEGIS-Desktop. ASAR SHA256: ed8028ece6fc2657798faae2e8897bee1caed31eb671c29e34f97fd4364a3881. The preceding application is backed up at X:/Future/ESCAPE/AEGIS-Desktop-before-duplicates-20260909; settings and Local Storage are backed up at X:/tmp/aegis-duplicates-profile-backup-20260909. Settings bytes were preserved during installation. Installed live-profile verification passed: 22 metrics, 6 received resource observations, CPU matched all 25 measured current processes, pause/resume and mutually exclusive Comparison/Table views. Settings bytes and theme/motion/scale preferences remained unchanged; no renderer errors occurred.
